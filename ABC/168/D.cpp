
#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  vector<int> a(m + 1, -1), b(m + 1, -1);
  for (int i = 0; i < m; ++i) {
    cin >> a.at(i + 1) >> b.at(i + 1);
  }
  for (int i = 0; i < n; ++i) {
  }
}