#include <bits/stdc++.h>
using namespace std;

int main() {
  string s;
  cin >> s;

  set<string> values;
  for (int i = 0; i < s.size(); ++i) {
    values.insert(s.at(i));
  }
  vector<int> ch(26);
  for (int i = 0; i < 26; ++i) {
    ch.emplace_back
  }
}