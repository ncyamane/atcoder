#include <bits/stdc++.h>
using namespace std;

int main()
{
    int64_t N, K;
    cin >> N >> K;

    int64_t sum = 0;
    for (int i = K; i < N; ++i)
    {
    }
    int64_t ans = pow(10, 10) * K + sum;
    int64_t mod = pow(10, 9) + 7;
}