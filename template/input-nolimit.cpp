#include <bits/stdc++.h>
using namespace std;

int main()
{
    // 文字数指定のない入力
    int input, i = 0, a[10];
    while (cin >> input)
    {
        a[i] = input;
        cout << a[i] << endl;
        i++;
    }
}